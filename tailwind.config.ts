import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#e11d48", // red
        dark: "#0f172a",    // black/dark
      },
    },
  },
  plugins: [],
};
export default config;